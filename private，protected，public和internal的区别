private：只能在本类中使用
protected：在本类中及其子类中可以使用
internal：同一命名空间（程序集）中的类可以使用
protected internal：上述两者的并集
public：所有类均可使用

从公开到私有
public->protected internal->protected->
                                        private
                          ->internal ->
