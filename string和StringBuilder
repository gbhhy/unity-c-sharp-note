//将char[]转化为string
string str=new string(char[]);

//将string以特定char分割为若干个字符串数组(string[])
string[] string.Split(char separator)
for example:
string str="fuck me";
sepStr[]=str.Split(' ');

//将string转化为stringbuilder
StringBuilder newBuilder = new StringBuilder(128);
newBuilder.Append(str);
